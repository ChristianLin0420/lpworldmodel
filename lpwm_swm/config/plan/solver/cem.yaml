_target_: stable_worldmodel.planning.solver.CEMSolver
cost: ???          # filled at runtime with a ShootingCostEvaluator(model, objective)
batch_size: 1
num_samples: 300
var_scale: 1.0
n_steps: 30
topk: 30
device: "cuda"
seed: ${seed}

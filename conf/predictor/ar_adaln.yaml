_target_: models.infojepa_modules.ARPredictor
# input_dim / hidden_dim / output_dim / num_frames are injected by train.py
# (they all equal the encoder emb_dim for adaln conditioning).
depth: 6
heads: 16
mlp_dim: 2048
dim_head: 64
dropout: 0.1
emb_dropout: 0.0
pred_proj_hidden: 2048

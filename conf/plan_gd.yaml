defaults:
  - _self_
  - override hydra/launcher: submitit_slurm

# Planning eval for LeWM-style (adaln) checkpoints. Env/dataset/encoder are
# loaded from the run's saved hydra.yaml, so this config is env-agnostic — just
# point model_name at the run dir under ${ckpt_base_path}/outputs/.
# The objective is visual-only (adaln drops proprio, so alpha is inert here).

hydra:
  run:
    dir: plan_outputs/${now:%Y%m%d%H%M%S}_${replace_slash:${model_name}}_gH${goal_H}
  sweep:
    dir: plan_outputs/${now:%Y%m%d%H%M%S}_${replace_slash:${model_name}}_gH${goal_H}
    subdir: ${hydra.job.num}
  launcher:
    submitit_folder: ${hydra.sweep.dir}/.submitit/%j
    nodes: 1
    tasks_per_node: 1
    cpus_per_task: 16
    mem_gb: 256
    gres: "gpu:h100:1"
    qos: "explore"
    timeout_min: 720
    setup: ["export DEBUGVAR=$(scontrol show hostnames $SLURM_JOB_NODELIST)",
            export MASTER_ADDR="$(scontrol show hostnames $SLURM_JOB_NODELIST | head -n 1)",
            "export MASTER_PORT=$(for port in $(shuf -i 30000-65500 -n 20); do if [[ $(netstat -tupln 2>&1 | grep $port | wc -l) -eq 0 ]] ; then echo $port; break; fi; done;)",]

# model to load for planning
ckpt_base_path: ./checkpoints  # put absolute path here. Checkpoints loaded from ${ckpt_base_path}/outputs
model_name: null
model_epoch: latest
# Plan-time consensus (planning/ensemble.py): list M run dirs (optionally "<run>@<epoch>")
# to vote on every CEM candidate; model_name then only labels the output dir. null => the
# ordinary single-model eval, unchanged. Pair with objective._target_=create_vote_objective_fn.
ensemble_members: null

seed: 99
n_evals: 50
goal_source: 'dset'
goal_H: 5
n_plot_samples: 10

debug_dset_init: False

objective:
  _target_: planning.objectives.create_objective_fn
  alpha: 0            # visual-only (adaln has no proprio latent); inert for adaln
  base: 2
  mode: last

# ---- V2 (PiWM-gd): gradient planner as a first-class arm -------------------------
# Identical to conf/plan_lewm.yaml except for the sub_planner: same checkpoints, same
# objective (create_objective_fn, visual-only), same MPC shell, same goal_H. The only
# thing that changes is HOW the action sequence is optimised -- SGD through the
# differentiable rollout instead of CEM's 300-sample resampling. That is the point:
# it separates "the model is bad" from "CEM's sampling is bad".
#
# opt_steps/eval_every deliberately DIFFER from conf/planner/mpc_gd.yaml (1000/10).
# 1000/10 fires gd.py:122-131 one hundred times per plan() -- a hundred real-env,
# 50-episode pygame rollouts, times 10 MPC iters -- which does not fit the 3h55
# allocation. 300/100 gives 3 env evals per plan(), matching CEM's 30 opt_steps x
# eval_every=1 order of magnitude.
#
# The objective block above is left as create_objective_fn on purpose: GDPlanner
# asserts against objective_fn_vote, because argsort ranks have zero gradient.
planner:
  _target_: planning.mpc.MPCPlanner
  max_iter: 10
  n_taken_actions: 5        # overwritten to goal_H at plan.py:202
  sub_planner:
    target: planning.gd.GDPlanner   # mpc.py:44 copies `target` -> `_target_`
    horizon: 5              # overwritten to goal_H at plan.py:201
    action_noise: 0.003
    sample_type: randn
    lr: 1
    opt_steps: 300
    eval_every: 100
  name: mpc_gd

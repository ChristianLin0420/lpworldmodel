_target_: models.vit_encoder.ViTEncoder
image_size: ${img_size}
patch_size: 14
dim: ${embed_dim}
depth: 12
heads: 3
mlp_dim: 768
dim_head: 64
dropout: 0.0
emb_dropout: 0.0
feature: cls          # "cls" (single token / latent_ndim=1) or "patch"
proj_hidden: 2048
proj_dim: ${embed_dim}
name: vit_scratch
